version: '3.9'

services:
  web:
    image: node:20-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - NEXT_TELEMETRY_DISABLED=1
    env_file:
      - .env.development
    command: sh -c "npm ci && npm run dev"
    restart: unless-stopped

# Optional: local Postgres for experiments (Supabase Cloud is default for staging/prod)
#  db:
#    image: postgres:15-alpine
#    environment:
#      POSTGRES_PASSWORD: postgres
#      POSTGRES_DB: domo_ai
#    ports:
#      - "5432:5432"
#    volumes:
#      - pg_data:/var/lib/postgresql/data
#
#volumes:
#  pg_data:
